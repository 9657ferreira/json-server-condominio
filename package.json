{
  "name": "json-server-condominio",
  "version": "1.0.0",
  "description": "json-server para o projeto de PW - Gestão de Condomínios",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "engines": {
    "node": ">=14 <20"
  }
}
